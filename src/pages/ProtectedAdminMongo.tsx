import {useEffect} from 'react';import {useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '@/context/AuthContext';import AdminMongo from './AdminMongo';export default function ProtectedAdminMongo(){const{user,token}=useAuth();const nav=useNavigate();const loc=useLocation();useEffect(()=>{if(!token||!user)nav('/admin-login',{replace:true,state:{from:loc.pathname}});else if(user.role!=='admin')nav('/admin-login',{replace:true})},[token,user,nav,loc.pathname]);if(!token||!user||user.role!=='admin')return <div className="min-h-[70vh] flex items-center justify-center text-gray-500">Checking admin access…</div>;return <AdminMongo/>}
