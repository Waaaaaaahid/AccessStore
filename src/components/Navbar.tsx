import { useState,useEffect } from 'react';import {Link,useNavigate} from 'react-router-dom';import {Search,ShoppingCart,Menu,X,Coffee} from 'lucide-react';import {useCart} from '@/context/CartContext';import {useSettings} from '@/lib/config';
export default function Navbar(){const[scrolled,setScrolled]=useState(false);const[mobileOpen,setMobileOpen]=useState(false);const[searchOpen,setSearchOpen]=useState(false);const[searchQuery,setSearchQuery]=useState('');const{totalItems}=useCart();useSettings();const nav=useNavigate();useEffect(()=>{const f=()=>setScrolled(scrollY>12);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);const search=(e:React.FormEvent)=>{e.preventDefault();if(searchQuery.trim()){nav(`/store?q=${encodeURIComponent(searchQuery.trim())}`);setSearchOpen(false);setSearchQuery('')}};const links=[['Home','/'],['Store','/store'],['Categories','/store'],['Track Order','/track'],['Support','/support']];return <><header className={`fixed top-0 left-0 right-0 z-50 transition-all ${scrolled?'bg-white/95 backdrop-blur-md shadow-sm':'bg-white border-b border-slate-100'}`}><nav className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="flex items-center justify-between h-[68px]"><Link to="/" className="flex items-center gap-2.5"><div className="w-9 h-9 bg-slate-950 rounded-lg flex items-center justify-center"><div className="grid grid-cols-2 gap-1"><div className="w-2 h-2 bg-blue-500 rounded-sm"/><div className="w-2 h-2 bg-white rounded-sm"/><div className="w-2 h-2 bg-white rounded-sm"/><div className="w-2 h-2 bg-blue-500 rounded-sm"/></div></div><span className="font-bold text-lg">AccessStore</span></Link><div className="hidden lg:flex">{links.map(([n,p])=><Link key={n} to={p} className="px-3.5 py-2 text-[13px] font-medium text-slate-600 hover:text-blue-600">{n}</Link>)}</div><div className="flex items-center gap-1.5"><button onClick={()=>setSearchOpen(!searchOpen)} className="p-2 text-slate-600"><Search className="w-5"/></button><Link to="/cart" className="relative p-2 text-slate-600"><ShoppingCart className="w-5"/>{totalItems>0&&<span className="absolute -top-0.5 -right-0.5 bg-blue-600 text-white text-[10px] font-bold min-w-[17px] h-[17px] flex items-center justify-center rounded-full">{totalItems}</span>}</Link><Link to="/support" className="hidden sm:flex items-center gap-1.5 px-4 py-2 bg-slate-950 text-white text-sm font-semibold rounded-lg"><Coffee className="w-4"/>Support</Link><button onClick={()=>setMobileOpen(!mobileOpen)} className="lg:hidden p-2 text-slate-600">{mobileOpen?<X className="w-5"/>:<Menu className="w-5"/>}</button></div></div>{searchOpen&&<div className="py-3 border-t"><form onSubmit={search} className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4"/><input autoFocus value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} placeholder="Search products..." className="w-full pl-10 py-2.5 bg-slate-50 border rounded-lg"/></form></div>}</nav>{mobileOpen&&<div className="lg:hidden bg-white border-t shadow-lg"><div className="px-4 py-3">{links.map(([n,p])=><Link key={n} to={p} onClick={()=>setMobileOpen(false)} className="block px-4 py-2.5 text-sm text-slate-600">{n}</Link>)}</div></div>}</header><div className="h-[68px]"/></>}
